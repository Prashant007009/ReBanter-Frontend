import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export function NewDropScreen() {
  return <ScreenPlaceholder title="New drop" note="Composer: media, tone filter, caption, place, crew tag" />;
}
